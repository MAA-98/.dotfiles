# Common aliases

alias ls='ls -AG' # Escape with \ls to bypass alias
alias cptree='copytree --git-ignore --clipboard'
alias cpcont='copy-contents --clipboard'
alias t='tmux new -A -s main'

alias pbcp='pbcopy'
alias pbp='pbpaste'
alias tch='touch'
